<?xml version="1.0" encoding="UTF-8"?>
<!-- This is the top-level element in all Maven pom.xml files -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <!-- This element indicates what version of the object model this POM is using -->
    <modelVersion>4.0.0</modelVersion>

    <!-- This element indicates the unique identifier of the organizaiton or group that created the project -->
    <groupId>cn.edu.nju.smail.151250043</groupId>

    <!-- This element indicates the unique base name of the primary artifact being generated by this project. -->
    <artifactId>MyLex</artifactId>

    <properties>
        <maven.compiler.source>1.9</maven.compiler.source>
        <maven.compiler.target>1.9</maven.compiler.target>
    </properties>

    <!-- This element indicates the package type to be used by this artifact (e.g. JAR, WAR, EAR, etc.) -->
    <!-- The default value for the packaging element is JAR so you do not have to specify this for most projects. -->
    <packaging>jar</packaging>

    <!-- This element indicates the version of the artifact generated by the project. -->
    <version>1.0-SNAPSHOT</version>

    <!-- This element indicates the display name used for the project. -->
    <name>MyLex</name>

    <!-- This element provides a basic description of your project. This is often used in Maven's generated documentation. -->
    <description>词法分析器生成工具</description>

    <dependencies>
        <!-- For each external dependency, you'll need to define at least 4 things: groupId, artifactId, version, and scope.  -->

        <!-- https://mvnrepository.com/artifact/junit/junit -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.12</version>
            <!-- The scope element indicates how your project uses that dependency, and can be values like compile, test, and runtime.  -->
            <scope>test</scope>
        </dependency>

        <!-- https://mvnrepository.com/artifact/log4j/log4j -->
        <dependency>
            <groupId>log4j</groupId>
            <artifactId>log4j</artifactId>
            <version>1.2.17</version>
            <scope>compile</scope>
        </dependency>

    </dependencies>


</project>